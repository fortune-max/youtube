import styled from "styled-components";

export const PlaylistContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
`;

export const PlaylistName = styled.div`
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 10px;
    margin-top: 10px;
    font-family: "Roboto", sans-serif;
    color: #333;
`;

export const PlaylistView = styled.div`
    display: flex;
    gap: 20px;
`